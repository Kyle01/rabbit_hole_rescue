import React from 'react';
import { Route, Switch } from "react-router-dom";
import App from './app';

const Root = () => (
    <div className="page">
       <App />
    </div>
);

export default Root;
