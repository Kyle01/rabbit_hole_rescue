import { connect } from 'react-redux';

import Show from './show';

const msp = (state) => {

  return {};
};

// const mdp = dispatch => {
//   return {};
// };

export default connect(msp,null)(Show);
